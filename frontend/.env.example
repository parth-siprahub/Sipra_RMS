# Backend API base URL — must end with /api (same path Nginx proxies to FastAPI).
# Production: https://rms.siprahub.com/api
# Local: uvicorn with API_PREFIX /api
VITE_API_URL=http://localhost:8000/api
