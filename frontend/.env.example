# Backend base URL (no trailing slash). Local: point at uvicorn. Production behind Nginx:
#   • https://your.domain/api     if Nginx uses location /api/ → FastAPI (recommended)
#   • https://your.domain         if Nginx proxies /sows, /auth, … at site root (see frontend/nginx.conf)
VITE_API_URL=http://localhost:8000
